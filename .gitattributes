# Windows batch dosyalari CRLF olmali; git bunlara hic dokunmasin
*.cmd -text
*.bat -text

# Kabuk betikleri ve calistirici LF olmali (shebang satiri bozulmasin)
*.sh   text eol=lf
axs    text eol=lf

# Axs kaynaklari ve belgeler
*.axs  text
*.nyl  text
*.md   text
*.py   text eol=lf
